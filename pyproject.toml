[project]
name = "prebuilt-onnxruntime-mobile"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.5",
    "packaging>=25.0",
    "setuptools>=80.9.0",
]
